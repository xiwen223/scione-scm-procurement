<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scione.scm.bill.infrastructure.persistence.mybatis.mapper.PoSyncRecordItemMapper">

    <select id="selectByOrderNo" resultType="com.scione.scm.bill.infrastructure.persistence.mybatis.po.PoSyncRecordItemPO">
        SELECT *
        FROM po_sync_record_item
        WHERE purchase_order_no = #{purchaseOrderNo}
        ORDER BY id ASC
    </select>

    <delete id="deleteByOrderNo">
        DELETE FROM po_sync_record_item WHERE purchase_order_no = #{purchaseOrderNo}
    </delete>

    <insert id="batchInsert">
        INSERT INTO po_sync_record_item
        (purchase_order_no, lx_item_id, plan_sn, product_id, product_name, sku, fnsku, model,
        unit_price, amount, quantity_plan, quantity_real, quantity_receive,
        cases_num, quantity_per_case, pic_url,
        tax_rate, spu, spu_name, warehouse_name, expect_arrive_time, remark, attribute_json, sync_time)
        VALUES
        <foreach collection="items" item="it" separator=",">
            (#{it.purchaseOrderNo}, #{it.lxItemId}, #{it.planSn}, #{it.productId}, #{it.productName}, #{it.sku},
            #{it.fnsku}, #{it.model}, #{it.unitPrice}, #{it.amount}, #{it.quantityPlan}, #{it.quantityReal},
            #{it.quantityReceive},
            #{it.casesNum}, #{it.quantityPerCase}, #{it.picUrl},
            #{it.taxRate}, #{it.spu}, #{it.spuName}, #{it.warehouseName},
            #{it.expectArriveTime}, #{it.remark}, #{it.attributeJson}, #{it.syncTime})
        </foreach>
    </insert>

</mapper>