spring:
  application:
    name: scione-scm-procurement
  main:
    banner-mode: off
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

shipping-mark:
  max-data-rows: 50
  max-wps-zip-entries: 200
  max-wps-zip-entry-bytes: 5MB
  max-wps-zip-total-bytes: 30MB

